# Copy this file to .env and fill in your own key.
# Vite embeds VITE_-prefixed variables into the client bundle at BUILD TIME.
# This key will be visible in the compiled JavaScript — protect it with
# HTTP referrer restrictions in Google Cloud Console (see README.md).
VITE_GOOGLE_MAPS_API_KEY=YOUR_KEY_HERE
